# Copy this to .env and adjust as needed.
# Docker Compose will pick up .env automatically.

# Flask session signing key. Generate a real one for production:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-in-production

# These have sensible defaults inside the container — only set if overriding.
# DATABASE_URL=sqlite:////data/fastchannels.db
# REDIS_URL=redis://localhost:6379/0
