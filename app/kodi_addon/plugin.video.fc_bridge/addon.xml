<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="plugin.video.fc_bridge" name="FastChannels Bridge" version="1.0.1" provider-name="FastChannels">
  <requires>
    <import addon="xbmc.python" version="3.0.0"/>
  </requires>
  <extension point="xbmc.python.pluginsource" library="default.py">
    <provides>video</provides>
  </extension>
  <extension point="xbmc.addon.metadata">
    <summary lang="en_GB">FastChannels DRM resolver</summary>
    <description lang="en_GB">Thin resolver addon for the FastChannels HDMI-encoder bridge. Takes a manifest URL and a Widevine license URL as plugin:// query params and hands them to inputstream.adaptive for playback, so FastChannels can remote-trigger a specific DRM channel via a single JSON-RPC Player.Open call instead of hosting a temporary M3U file per request.</description>
    <platform>all</platform>
  </extension>
</addon>
